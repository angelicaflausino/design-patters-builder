/*namespace BuilderApp
{
   public sealed class PersonWithFunctionalBuilder : FunctionalBuilder<Person, PersonFunctionalBuilder>
   {
       public PersonFunctionalBuilder Called(string name) => Do(p => p.Name = name);
   }
}
*/

//Utilizado para quebrar o construtor e ser reutilizado dentro de diferentes configurações